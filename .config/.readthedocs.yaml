version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14" # Keep sync with CONTRIBUTING

python:
  install:
  - requirements: docs/requirements.txt

mkdocs:
  fail_on_warning: true
  configuration: mkdocs.yml
